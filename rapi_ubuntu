CAM


$ sudo nano /boot/firmware/config.txt
  // disable camera auto detect if uses non-official RipCAM V3
  camera_auto_detect=0
  // enable dtoverlay for CAM0
  dtoverlay=imx708,cam0
  // enable dtoverlay for CAM1 (Option)
  dtoverlay=imx708,cam1

$ sudo reboot now

// check if install well
$ sudo dmesg | grep -i imx708
  

$ sudo apt install -y rpicam-apps
$ rpicam-hello -t 0

// install python 3.13
$ sudo apt update
$ sudo apt install software-properties-common
$ sudo add-apt-repository ppa:deadsnakes/ppa
$ sudo apt update

$ sudo apt install python3.13 python3.13-venv python3.13-dev

$ cd ~/你的專案

$ python3.13 -m venv .venv
$ source .venv/bin/activate

$ python --version

// download pcie driver from hailo developer-zone
  hailort-pcie-driver_4.24.0_all.deb

// install driver
$ sudo dpkg -i hailort-pcie-driver_4.24.0_all.deb
// reboot
$ sudo reboot now

// load hailo kernel module
$ sudo modprobe hailo_pci

// check if load well
$ lsmod | grep hailo
// or
$ ls /dev/hailo*

// install hailort
// download hailort_4.24.0_arm64.deb from developer-zone
$ sudo dpkg -i hailort_4.24.0_arm64.deb

// confirm if install well
$ hailortcli --version
$ hailortcli fw-control identify

// check hardware and driver install automatically after reboot
$ sudo reboot now
$ lspci | grep -i hailo
$ lsmod | grep hailo
$ ls -l /dev/hailo*
$ hailortcli --version
$ hailortcli fw-control identify

// install tools
$ sudo apt install -y python3-venv python3-pip python3-dev git python3-opencv pyenv

git clone https://github.com/pyenv/pyenv.git ~/.pyenv
git clone https://github.com/pyenv/pyenv-doctor.git ~/.pyenv/plugins/pyenv-doctor
git clone https://github.com/pyenv/pyenv-update.git ~/.pyenv/plugins/pyenv-update

pyenv install 3.13
pyenv local 3.13
python --version

$ python3 -m venv ~/hailo-yolo --system-site-packages
$ source ~/hailo-yolo/bin/activate


